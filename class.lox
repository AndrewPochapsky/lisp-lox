class Point {
    init(x, y) {
        this.x = x;
        this.y = y;
    }

    add() {
        print this.x + this.y;
    }
}

var p = Point(1, 2);
p.add();

class Foo {
    init() {
        return;
    }
}

var foo = Foo();
print foo.init();
